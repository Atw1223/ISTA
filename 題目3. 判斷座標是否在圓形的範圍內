#include<stdio.h>
#include<math.h>

int main(){
    int n,w;
    while(scanf("%d %d", &n,&w)!=EOF){
        if(pow(n,2)+pow(w,2)>40000)
            printf("outside\n");
        else
            printf("inside\n");
    }

}
